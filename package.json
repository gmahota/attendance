{
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "dev": "cd backend && yarn dev",
    "start": "cd backend && node dist/index.js",
    "heroku-prebuild": "cd backend && npm install && npm run postinstall",
    "build": "cd backend && npm install && npm run build",
    "web-dev":"cd web && yarn dev"
  }
}
